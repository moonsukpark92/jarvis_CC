# Development & test dependencies. Not required for `python -m jarvis_v5.jarvis`.

-r requirements.txt

# Test
pytest>=8.0
pytest-asyncio>=0.23
pytest-mock>=3.12

# Lint / format / types
ruff>=0.5
mypy>=1.10

# Build
pyinstaller>=6.6
